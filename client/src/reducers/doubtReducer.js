import { saveDoubt } from "../actions";
const initialState={title: "", description: ""} 

const createDoubt = (state=initialState, action ) => {
    switch(action.type) {
        case "askDoubt" : return 
        case "owais" : return
        default: return state
    }
}

export default createDoubt;